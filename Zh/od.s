(quote "该程序的作用是以二进制模式打印文件")
(define (f (open "luac.out" "rb")))
(define (s (f.read)))
(print s)

(quote
' '.join(format(ord(x), 'x') for x in st)
)

(define (t ";"))

(print (t.join (' "hello" "world")))